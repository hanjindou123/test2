<%= form_for(@available) do |f| %>
  <% if @available.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@available.errors.count, "error") %> prohibited this available from being saved:</h2>

      <ul>
      <% @available.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :instrument_id %><br>
    <%= f.number_field :instrument_id %>
  </div>
  <div class="field">
    <%= f.label :available_datetime %><br>
    <%= f.text_field :available_datetime %>
  </div>
  <div class="field">
    <%= f.label :available_timeslot %><br>
    <%= f.number_field :available_timeslot %>
  </div>
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
